services:
  - type: web
    name: lr1-parser-engine
    env: docker
    branch: main
    plan: free
    dockerfilePath: Dockerfile
    buildCommand: ""
    startCommand: "gunicorn -b 0.0.0.0:5000 app:app --workers=2"
    envVars: []
